version: '3.8'

services:
  dxfer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - DXFER_MOCK=0
    volumes:
      - ./web/uploads:/app/web/uploads
    restart: unless-stopped
